[package]
name = "mc-essais"
version.workspace = true
edition.workspace = true
license.workspace = true
# Outillage de test : jamais publié, jamais embarqué dans un binaire.
publish = false

[dependencies]
tokio = { workspace = true, features = ["net", "io-util", "rt", "macros"] }
