/target

# Secrets : la clé d'API CurseForge est nominative et ne se versionne pas.
.env
*.key

# Rapport de couverture produit par cargo-llvm-cov.
/lcov.info

# Le front : dépendances, sortie de build, cache du compilateur Angular.
/node_modules
/dist
/.angular
